# 九九の表を表示するPythonコード

# 1から9までの数字を使って九九の表を作成
for i in range(1, 10):  # 1から9まで繰り返し
    for j in range(1, 10):  # 1から9まで繰り返し
        # 乗算結果を表示。フォーマット指定で整えた表示にする
        print(f"{i} × {j} = {i * j}", end="\t")  # tabで区切って表示
    print()  # 各行が終わったら改行を入れる
