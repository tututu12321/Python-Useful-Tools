# 必要なライブラリを再インポート（実行環境がリセットされたため）
# 変数の定義
P1 = 50.5 * 10**3  # 初期圧力 (Pa)
V1 = 200 / 1000     # 初期体積 (L)
P2 = (500 / 760) * (1.01 * 10**5)  # 変化後の圧力 (Pa)

# ボイルの法則を適用して V2 を計算
V2 = (P1 * V1) / P2  # 変化後の体積 (L)

# mL に変換
V2_ml = V2 * 1000

# 結果を表示
print(f"求める体積 V: {V2_ml:.2f} mL")
